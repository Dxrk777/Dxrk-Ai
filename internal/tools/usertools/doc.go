// SPDX-License-Identifier: MIT
// Package usertools provides tools for user interaction — asking questions
// and sending proactive messages. These tools decouple the LLM execution
// layer from the presentation layer (TUI, Web, etc.) via the UserHandler
// interface, allowing the same tool definitions to work across frontends.
package usertools
